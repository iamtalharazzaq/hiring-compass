import { Link } from "react-router-dom";
import { useCandidateApplications } from "../../features/applications/queries";
export function ApplicationHistory({ organizationId, candidateId }: { organizationId: string; candidateId: string }) { const { data } = useCandidateApplications(organizationId, candidateId); return <section className="mt-8 rounded-2xl border bg-[var(--color-surface)] p-6"><h2 className="font-semibold">Applications</h2>{!data?.items.length ? <p className="mt-3 text-sm text-[var(--color-muted)]">This candidate has not been connected to a job yet.</p> : <div className="mt-3 space-y-2">{data.items.map((item) => <Link key={item.id} to={`/jobs/${item.job_id}`} className="block rounded-xl border p-3 text-sm"><strong>Job {item.job_id.slice(0, 8)}</strong><span className="ml-3 text-[var(--color-muted)]">{item.status}</span></Link>)}</div>}</section>; }
