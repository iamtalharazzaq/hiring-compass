import type { JobStatus } from "../../features/jobs/types";
const labels: Record<JobStatus, string> = { draft: "Draft", pending_approval: "In review", approved: "Approved", closed: "Closed", archived: "Archived" };
export function JobStatusBadge({ status }: { status: JobStatus }) { const tone = status === "approved" ? "bg-[#dcfce7] text-[#2c955c]" : status === "draft" ? "hc-job-status-badge--draft" : "bg-[#e2e2ff] text-[#4746e5]"; return <span className={`hc-job-status-badge inline-flex min-h-6 min-w-20 items-center justify-center rounded-full px-2.5 py-1 text-xs font-semibold ${tone}`}>{labels[status]}</span>; }
