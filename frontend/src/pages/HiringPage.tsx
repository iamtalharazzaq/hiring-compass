import { useSearchParams } from "react-router-dom";
import { AppShell } from "../components/layout/AppShell";
import { JobsList } from "./JobsPage";
import { CandidatesList } from "./CandidatesPage";
export function HiringPage() {
  const [params, setParams] = useSearchParams(); const tab = params.get("tab") === "candidates" ? "candidates" : "jobs"; const choose = (next: string) => setParams({ tab: next });
  return <AppShell title="Hiring"><div className="inline-flex rounded-full bg-[var(--color-sage)] p-1" role="tablist" aria-label="Hiring workspace"><button type="button" role="tab" aria-selected={tab === "jobs"} onClick={() => choose("jobs")} className={`min-w-28 rounded-full px-5 py-2.5 text-sm font-semibold transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--color-navy)] focus-visible:ring-offset-2 ${tab === "jobs" ? "bg-[var(--color-surface)] text-[var(--color-navy)] shadow-sm" : "text-[var(--color-teal)] hover:bg-[var(--color-surface)]/40"}`}>Jobs</button><button type="button" role="tab" aria-selected={tab === "candidates"} onClick={() => choose("candidates")} className={`min-w-28 rounded-full px-5 py-2.5 text-sm font-semibold transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--color-navy)] focus-visible:ring-offset-2 ${tab === "candidates" ? "bg-[var(--color-surface)] text-[var(--color-navy)] shadow-sm" : "text-[var(--color-teal)] hover:bg-[var(--color-surface)]/40"}`}>Candidates</button></div><section className="mt-6">{tab === "jobs" ? <JobsList /> : <CandidatesList />}</section></AppShell>;
}
