import { useSearchParams } from "react-router-dom";
import { AppShell } from "../components/layout/AppShell";
import { JobsList } from "./JobsPage";
import { CandidatesList } from "./CandidatesPage";
export function HiringPage() {
  const [params, setParams] = useSearchParams(); const tab = params.get("tab") === "candidates" ? "candidates" : "jobs"; const choose = (next: string) => setParams({ tab: next });
  return <AppShell title="Hiring"><div className="inline-flex rounded-full border border-[var(--color-tab-border)] bg-[var(--color-tab-track)] p-1 shadow-sm" role="tablist" aria-label="Hiring workspace"><button type="button" role="tab" aria-selected={tab === "jobs"} onClick={() => choose("jobs")} className={`min-w-28 rounded-full px-5 py-2.5 text-sm font-semibold transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--color-tab-active)] focus-visible:ring-offset-2 ${tab === "jobs" ? "bg-[var(--color-tab-active)] text-white shadow-sm hover:bg-[var(--color-tab-active-hover)]" : "text-[var(--color-tab-text)] hover:bg-[color-mix(in_srgb,var(--color-tab-active)_14%,transparent)]"}`}>Jobs</button><button type="button" role="tab" aria-selected={tab === "candidates"} onClick={() => choose("candidates")} className={`min-w-28 rounded-full px-5 py-2.5 text-sm font-semibold transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--color-tab-active)] focus-visible:ring-offset-2 ${tab === "candidates" ? "bg-[var(--color-tab-active)] text-white shadow-sm hover:bg-[var(--color-tab-active-hover)]" : "text-[var(--color-tab-text)] hover:bg-[color-mix(in_srgb,var(--color-tab-active)_14%,transparent)]"}`}>Candidates</button></div><section className="mt-6">{tab === "jobs" ? <JobsList /> : <CandidatesList />}</section></AppShell>;
}
