import { z } from "zod";
const optionalNumber = z.preprocess((value) => value === "" ? undefined : Number(value), z.number().min(0).optional());
export const jobSchema = z.object({ title: z.string().trim().min(3).max(160), description: z.string().optional(), location: z.string().max(160).optional(), employment_type: z.string().optional(), workplace_type: z.string().optional(), experience_min_years: optionalNumber, experience_max_years: optionalNumber, salary_min: optionalNumber, salary_max: optionalNumber, salary_currency: z.string().trim().toUpperCase().optional() }).superRefine((value, ctx) => { if (value.experience_min_years !== undefined && value.experience_max_years !== undefined && value.experience_max_years < value.experience_min_years) ctx.addIssue({ code: "custom", path: ["experience_max_years"], message: "Must be at least the minimum." }); if (value.salary_min !== undefined && value.salary_max !== undefined && value.salary_max < value.salary_min) ctx.addIssue({ code: "custom", path: ["salary_max"], message: "Must be at least the minimum." }); if ((value.salary_min !== undefined || value.salary_max !== undefined) && !value.salary_currency) ctx.addIssue({ code: "custom", path: ["salary_currency"], message: "Currency is required with salary." }); if (value.salary_currency && !/^[A-Z]{3}$/.test(value.salary_currency)) ctx.addIssue({ code: "custom", path: ["salary_currency"], message: "Use a three-letter currency code." }); });
