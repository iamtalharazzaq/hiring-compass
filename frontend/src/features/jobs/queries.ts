import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { approveJob, archiveJob, closeJob, createJob, createRequirement, deleteRequirement, getJob, listJobs, listRequirements, reorderRequirements, returnJobToDraft, submitJob, updateJob, updateRequirement } from "./api";
import type { JobInput, JobStatus, RequirementInput } from "./types";
export const jobKeys = { list: (org: string, page: number, status?: JobStatus, search?: string) => ["jobs", org, page, status, search] as const, detail: (org: string, id: string) => ["jobs", org, id] as const, requirements: (org: string, id: string) => ["jobs", org, id, "requirements"] as const };
export const useJobs = (org: string, page: number, status?: JobStatus, search?: string) => useQuery({ queryKey: jobKeys.list(org, page, status, search), queryFn: () => listJobs(org, { page, status, search }), enabled: Boolean(org) });
export const useJob = (org: string, id: string) => useQuery({ queryKey: jobKeys.detail(org, id), queryFn: () => getJob(org, id), enabled: Boolean(org && id) });
export const useRequirements = (org: string, id: string) => useQuery({ queryKey: jobKeys.requirements(org, id), queryFn: () => listRequirements(org, id), enabled: Boolean(org && id) });
export function useJobMutation(org: string) { const client = useQueryClient(); const refresh = () => client.invalidateQueries({ queryKey: ["jobs", org] }); const refreshRequirements = () => { refresh(); return client.invalidateQueries({ queryKey: ["jobs", org] }); }; return { create: useMutation({ mutationFn: (input: JobInput) => createJob(org, input), onSuccess: refresh }), update: useMutation({ mutationFn: ({ id, input }: { id: string; input: Partial<JobInput> }) => updateJob(org, id, input), onSuccess: refresh }), close: useMutation({ mutationFn: (id: string) => closeJob(org, id), onSuccess: refresh }), archive: useMutation({ mutationFn: (id: string) => archiveJob(org, id), onSuccess: refresh }), submit: useMutation({ mutationFn: (id: string) => submitJob(org, id), onSuccess: refresh }), approve: useMutation({ mutationFn: (id: string) => approveJob(org, id), onSuccess: refresh }), returnToDraft: useMutation({ mutationFn: ({ id, note }: { id: string; note: string }) => returnJobToDraft(org, id, note), onSuccess: refresh }), createRequirement: useMutation({ mutationFn: ({ jobId, input }: { jobId: string; input: RequirementInput }) => createRequirement(org, jobId, input), onSuccess: refreshRequirements }), updateRequirement: useMutation({ mutationFn: ({ jobId, requirementId, input }: { jobId: string; requirementId: string; input: RequirementInput }) => updateRequirement(org, jobId, requirementId, input), onSuccess: refreshRequirements }), deleteRequirement: useMutation({ mutationFn: ({ jobId, requirementId }: { jobId: string; requirementId: string }) => deleteRequirement(org, jobId, requirementId), onSuccess: refreshRequirements }), reorderRequirements: useMutation({ mutationFn: ({ jobId, ids }: { jobId: string; ids: string[] }) => reorderRequirements(org, jobId, ids), onSuccess: refreshRequirements }) }; }
