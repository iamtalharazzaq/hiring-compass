import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { createCandidate, downloadResume, getCandidate, listCandidates, listResumes, updateCandidate, uploadResume } from "./api";
import type { CandidateInput } from "./types";
export const candidateKeys = { list: (org: string, page: number, search?: string) => ["candidates", org, page, search] as const, detail: (org: string, id: string) => ["candidates", org, id] as const };
export const useCandidates = (org: string, page: number, search?: string, filters?: { location?: string; current_title?: string; min_years?: number; max_years?: number }) => useQuery({ queryKey: ["candidates", org, page, search, filters], queryFn: () => listCandidates(org, page, search, filters), enabled: Boolean(org) });
export const useCandidate = (org: string, id: string) => useQuery({ queryKey: candidateKeys.detail(org, id), queryFn: () => getCandidate(org, id), enabled: Boolean(org && id) });
export const useResumes = (org: string, id: string) => useQuery({ queryKey: ["candidates", org, id, "resumes"], queryFn: () => listResumes(org, id), enabled: Boolean(org && id) });
export function useCandidateMutation(org: string) { const client = useQueryClient(); const refresh = () => client.invalidateQueries({ queryKey: ["candidates", org] }); return { create: useMutation({ mutationFn: (input: CandidateInput) => createCandidate(org, input), onSuccess: refresh }), update: useMutation({ mutationFn: ({ id, input }: { id: string; input: Partial<CandidateInput> }) => updateCandidate(org, id, input), onSuccess: refresh }), upload: useMutation({ mutationFn: ({ id, file }: { id: string; file: File }) => uploadResume(org, id, file), onSuccess: refresh }), download: useMutation({ mutationFn: ({ candidateId, resumeId }: { candidateId: string; resumeId: string }) => downloadResume(org, candidateId, resumeId) }) }; }
